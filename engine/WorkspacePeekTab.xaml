<Window x:Class="HiveMind.AgentWorkspaces.WorkspacePeekTab" x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" ResizeMode="NoResize" Topmost="True" ShowActivated="False"
        UseLayoutRounding="True" Title="Workspace edge tab">
    <Button x:Name="RevealButton" Cursor="Hand" Click="Reveal_Click"
            MouseEnter="Reveal_MouseEnter" MouseLeave="Reveal_MouseLeave"
            Foreground="{DynamicResource InkBrush}"
            FocusVisualStyle="{StaticResource KeyboardFocus}">
        <Button.Template>
            <ControlTemplate TargetType="Button">
                <Border x:Name="Surface" Background="{DynamicResource CardBrush}"
                        BorderBrush="{DynamicResource GlassEdgeBrush}" BorderThickness="1" CornerRadius="8">
                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter TargetName="Surface" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                    </Trigger>
                    <Trigger Property="IsKeyboardFocused" Value="True">
                        <Setter TargetName="Surface" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter TargetName="Surface" Property="Opacity" Value="0.8"/>
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>
        </Button.Template>
        <StackPanel>
            <Path Data="M 1,3 L 10,3 L 10,10 L 1,10 Z M 4,0 L 13,0 L 13,7"
                  Stroke="{DynamicResource MutedInkBrush}" StrokeThickness="1.2"
                  Width="14" Height="11" Margin="0,0,0,10"/>
            <Path x:Name="Direction" Stroke="{DynamicResource InkBrush}" StrokeThickness="1.6"
                  StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"
                  HorizontalAlignment="Center" Width="6" Height="10"/>
        </StackPanel>
    </Button>
</Window>
